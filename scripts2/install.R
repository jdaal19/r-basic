install.packages("tidyverse",dep = TRUE) 

# instala paquetes por consola y todos
#sus paquetes asociados para su funcionamiento

library(tidyverse)

#activa el paquete y todos los paquetes asociados para el funcionamiento.

install.packages("magic", dep = TRUE)
library(magic)

magic(6)

installed.packages()
